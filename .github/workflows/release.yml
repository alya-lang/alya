name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Release tag (e.g. v0.0.2)'
        required: true
        default: 'v0.0.2'
      prerelease:
        description: 'Mark as prerelease'
        type: boolean
        required: true
        default: false

permissions:
  contents: write

jobs:
  build:
    name: Build (${{ matrix.label }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            target: x86_64-unknown-linux-gnu
            platform: x86_64-linux
            label: Linux x64
            bin: alya
            archive: tar.gz
          - os: ubuntu-24.04-arm
            target: aarch64-unknown-linux-gnu
            platform: arm64-linux
            label: Linux ARM64
            bin: alya
            archive: tar.gz
          - os: macos-latest
            target: x86_64-apple-darwin
            platform: x86_64-macos
            label: macOS Intel
            bin: alya
            archive: tar.gz
          - os: macos-latest
            target: aarch64-apple-darwin
            platform: arm64-macos
            label: macOS ARM64
            bin: alya
            archive: tar.gz
          - os: windows-latest
            target: x86_64-pc-windows-msvc
            platform: x86_64-windows
            label: Windows x64
            bin: alya.exe
            archive: zip
            tc: alya-toolchain-windows-x64.zip
          - os: windows-11-arm
            target: aarch64-pc-windows-msvc
            platform: arm64-windows
            label: Windows ARM64
            bin: alya.exe
            archive: zip
            tc: alya-toolchain-windows-arm64.zip

    steps:
      - name: Checkout repository
        uses: actions/checkout@v7

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: ${{ matrix.target }}

      - name: Build binary
        run: cargo build --release --target ${{ matrix.target }}

      - name: Package artifacts (Unix)
        if: matrix.archive == 'tar.gz'
        shell: bash
        run: |
          VERSION="${{ github.ref_type == 'tag' && github.ref_name || inputs.tag }}"
          PACKAGE_NAME="alya-${VERSION}-${{ matrix.platform }}"
          mkdir "${PACKAGE_NAME}"
          cp "target/${{ matrix.target }}/release/${{ matrix.bin }}" "${PACKAGE_NAME}/"
          cp README.md LICENSE "${PACKAGE_NAME}/"
          tar -czvf "${PACKAGE_NAME}.tar.gz" "${PACKAGE_NAME}"
          shasum -a 256 "${PACKAGE_NAME}.tar.gz" > "${PACKAGE_NAME}.tar.gz.sha256"

      - name: Package artifacts (Windows)
        if: matrix.archive == 'zip'
        shell: pwsh
        run: |
          $Version = if ("${{ github.ref_type }}" -eq "tag") { "${{ github.ref_name }}" } else { "${{ inputs.tag }}" }
          
          # 1. Standard lightweight package
          $PackageName = "alya-$Version-${{ matrix.platform }}"
          New-Item -ItemType Directory -Force -Path $PackageName | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/${{ matrix.bin }}" -Destination $PackageName
          Copy-Item README.md, LICENSE -Destination $PackageName
          Compress-Archive -Path $PackageName -DestinationPath "$PackageName.zip"
          $hash = (Get-FileHash -Algorithm SHA256 "$PackageName.zip").Hash.ToLower()
          "$hash  $PackageName.zip" | Out-File -FilePath "$PackageName.zip.sha256" -Encoding ascii

          # 2. Standalone offline package (with pre-bundled toolchain)
          $StandaloneName = "alya-$Version-${{ matrix.platform }}-standalone"
          New-Item -ItemType Directory -Force -Path $StandaloneName | Out-Null
          Copy-Item "target/${{ matrix.target }}/release/${{ matrix.bin }}" -Destination $StandaloneName
          Copy-Item README.md, LICENSE -Destination $StandaloneName
          
          # Download and bundle minimal toolchain (tracking latest release, with fallback)
          $tcUrl = "https://github.com/alya-lang/toolchain/releases/latest/download/${{ matrix.tc }}"
          $tcZip = Join-Path $StandaloneName "toolchain.zip"
          $tcDir = Join-Path $StandaloneName "toolchain"
          try {
              [Net.ServicePointManager]::SecurityProtocol = [Net.SecurityProtocolType]::Tls12
              try {
                  Invoke-WebRequest -Uri $tcUrl -OutFile $tcZip -UseBasicParsing
              } catch {
                  Write-Host "Latest toolchain download failed, falling back to v1.0.0: $_"
                  Invoke-WebRequest -Uri "https://github.com/alya-lang/toolchain/releases/download/v1.0.0/${{ matrix.tc }}" -OutFile $tcZip -UseBasicParsing
              }
              Expand-Archive -Path $tcZip -DestinationPath $tcDir -Force
              Remove-Item -Force $tcZip
          } catch {
              Write-Host "Warning: Could not fetch pre-bundled toolchain for standalone package: $_"
          }

          Compress-Archive -Path $StandaloneName -DestinationPath "$StandaloneName.zip"
          $hashStandalone = (Get-FileHash -Algorithm SHA256 "$StandaloneName.zip").Hash.ToLower()
          "$hashStandalone  $StandaloneName.zip" | Out-File -FilePath "$StandaloneName.zip.sha256" -Encoding ascii

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v7
        with:
          name: release-${{ matrix.platform }}
          path: |
            alya-*-${{ matrix.platform }}.*
          if-no-files-found: error

  publish:
    name: Publish Release
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v7
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v7
        with:
          python-version: '3.x'

      - name: Download all release artifacts
        uses: actions/download-artifact@v8
        with:
          pattern: release-*
          merge-multiple: true
          path: dist

      - name: Generate Release Notes
        id: notes
        run: |
          TAG="${{ github.ref_type == 'tag' && github.ref_name || inputs.tag }}"
          python3 .github/scripts/generate_release_notes.py "$TAG"

      - name: Create or Update GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          name: ${{ steps.notes.outputs.title }}
          tag_name: ${{ steps.notes.outputs.tag }}
          prerelease: ${{ inputs.prerelease == true || (inputs.prerelease != false && contains(steps.notes.outputs.tag, '-')) }}
          body_path: RELEASE_NOTES.md
          generate_release_notes: false
          files: dist/*

      - name: Update Distribution Manifests and Push Checksums
        run: |
          TAG="${{ steps.notes.outputs.tag }}"
          WIN_SHA=$(cut -d' ' -f1 < dist/alya-${TAG}-x86_64-windows.zip.sha256)
          echo "Updating distribution manifests for $TAG with Windows ZIP SHA-256: $WIN_SHA"

          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          git checkout main
          git pull origin main --rebase

          python3 .github/scripts/update_distribution_checksums.py "$TAG" "$WIN_SHA"

          if [ -n "$(git status --porcelain packages/distribution)" ]; then
            git add packages/distribution
            git commit -m "chore(distribution): record $TAG release checksums for winget, choco, and scoop [skip ci]"
            git push origin main

            git fetch origin develop
            git checkout develop
            git pull origin develop --rebase
            git merge main -m "chore: sync $TAG distribution checksums to develop [skip ci]"
            git push origin develop
          else
            echo "Distribution manifests are already up-to-date."
          fi
