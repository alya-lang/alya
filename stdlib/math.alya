# Alya Standard Library - Math Module
# Provides common mathematical constants, trigonometry, statistics, and utility functions.

pub let PI = 3.141592653589793
pub let E = 2.718281828459045
pub let TAU = 6.283185307179586
pub let SQRT2 = 1.4142135623730951
pub let LN2 = 0.6931471805599453
pub let LN10 = 2.302585092994046
pub let PHI = 1.618033988749895

pub function deg_to_rad(deg)
    return deg * (3.141592653589793 / 180.0)
end

pub function radians(deg)
    return deg * (3.141592653589793 / 180.0)
end

pub function rad_to_deg(rad)
    return rad * (180.0 / 3.141592653589793)
end

pub function degrees(rad)
    return rad * (180.0 / 3.141592653589793)
end

pub function clamp(val, min_val, max_val)
    when val
        is < min_val then return min_val
        is > max_val then return max_val
        else return val
    end
end

pub function sign(val)
    when val
        is > 0 then return 1
        is < 0 then return -1
        else return 0
    end
end

pub function is_even(n)
    return n % 2 == 0
end

pub function is_odd(n)
    return n % 2 != 0
end

pub function sqrt_f(x)
    return native_sqrt(float(x))
end

pub function hypot(a, b)
    let fa = float(a)
    let fb = float(b)
    return native_sqrt(fa * fa + fb * fb)
end

pub function round(val)
    when val
        is >= 0.0 then return int(val + 0.5)
        else return int(val - 0.5)
    end
end

pub function floor(val)
    let iv = int(val)
    if val >= 0.0 or float(iv) == val
        return iv
    else
        return iv - 1
    end
end

pub function ceil(val)
    let iv = int(val)
    if val <= 0.0 or float(iv) == val
        return iv
    else
        return iv + 1
    end
end

pub function trunc(val)
    return int(val)
end

function _normalize_angle(rad)
    let two_pi = 6.283185307179586
    let pi = 3.141592653589793
    let cycles = int(rad / two_pi)
    let rem = rad - float(cycles) * two_pi
    while rem > pi
        rem = rem - two_pi
    end
    while rem < -pi
        rem = rem + two_pi
    end
    return rem
end

pub function sin(x)
    return native_sin(float(x))
end

pub function cos(x)
    return native_cos(float(x))
end

pub function tan(x)
    return native_tan(float(x))
end

pub function asin(x)
    return native_asin(float(x))
end

pub function acos(x)
    return native_acos(float(x))
end

pub function atan(x)
    return native_atan(float(x))
end

pub function atan2(y, x)
    return native_atan2(float(y), float(x))
end

pub function sinh(x)
    return native_sinh(float(x))
end

pub function cosh(x)
    return native_cosh(float(x))
end

pub function tanh(x)
    return native_tanh(float(x))
end

pub function log_n(x)
    return native_log(float(x))
end

pub function log2(x)
    return native_log2(float(x))
end

pub function log10(x)
    return native_log10(float(x))
end

pub function exp_f(x)
    return native_exp(float(x))
end

pub function fmod(a, b)
    return native_fmod(float(a), float(b))
end

pub function rand_range(min_val, max_val)
    if min_val >= max_val
        return min_val
    end
    let r = rand()
    let span = max_val - min_val + 1
    return min_val + (r % span)
end

pub function sum(arr)
    let total = 0
    for x in arr
        total += x
    end
    return total
end

pub function mean(arr)
    let count = len(arr)
    if count == 0
        return 0.0
    end
    return float(sum(arr)) / float(count)
end

pub function median(arr)
    let count = len(arr)
    if count == 0
        return 0
    end
    let sorted = []
    for x in arr
        sorted.push(x)
    end
    let j = 1
    let key, k = 0
    while j < count
        key = sorted[j]
        k = j - 1
        while k >= 0 and sorted[k] > key
            sorted[k + 1] = sorted[k]
            k -= 1
        end
        sorted[k + 1] = key
        j += 1
    end
    let mid = count / 2
    if count % 2 != 0
        return sorted[mid]
    else
        return (sorted[mid - 1] + sorted[mid]) / 2
    end
end

pub function variance(arr)
    let count = len(arr)
    if count == 0
        return 0.0
    end
    let m = mean(arr)
    let sum_sq = 0.0
    let diff = 0.0
    for x in arr
        diff = float(x) - m
        sum_sq += diff * diff
    end
    return sum_sq / float(count)
end

pub function std_dev(arr)
    return native_sqrt(variance(arr))
end

pub function lerp(a, b, t)
    return a + (b - a) * t
end

pub function norm(val, min_val, max_val)
    let span = max_val - min_val
    if span == 0.0 or span == 0
        return 0.0
    end
    return (val - min_val) / span
end

pub function smoothstep(edge0, edge1, x)
    let span = edge1 - edge0
    if span == 0.0 or span == 0
        return 0.0
    end
    let t = (x - edge0) / span
    if t < 0.0
        return 0.0
    end
    if t > 1.0
        return 1.0
    end
    return t * t * (3.0 - 2.0 * t)
end

pub function gcd(a, b)
    let x = abs(a)
    let y = abs(b)
    let temp = 0
    while y != 0
        temp = y
        y = x % y
        x = temp
    end
    return x
end

pub function lcm(a, b)
    if a == 0 or b == 0
        return 0
    end
    let g = gcd(a, b)
    let x = abs(a)
    let y = abs(b)
    return (x / g) * y
end

pub function factorial(n)
    if n <= 1
        return 1
    end
    let res = 1
    let i = 2
    while i <= n
        res *= i
        i += 1
    end
    return res
end

pub function is_prime(n)
    if n <= 1
        return 0
    end
    if n <= 3
        return 1
    end
    if n % 2 == 0 or n % 3 == 0
        return 0
    end
    let i = 5
    while i * i <= n
        if n % i == 0 or n % (i + 2) == 0
            return 0
        end
        i += 6
    end
    return 1
end
